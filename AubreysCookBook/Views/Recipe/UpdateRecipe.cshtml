@model Recipe

<h2>Update @Model.Name</h2>

@using (Html.BeginForm("UpdateRecipeToDatabase", "Recipe", "Post"))
{
    <input asp-for="RecipeID" type="hidden" value="@Model.RecipeID" />

    <label asp-for="Name" class="control-label">Name</label>
    <input id="TableRow" type="text" name="Name" class="form-control" value="@Model.Name" />

    <label asp-for="RecipeDescription" class="control-label">Recipe Description</label>
    <input id="TableRow" type="text" step="0.01" name="RecipeDescription" class="form-control" value="@Model.RecipeDescription" />

    <label asp-for="Ingredients" class="control-label">Ingredients</label>
    <input id="TableRow" type="text" step="0.01" name="Ingredients" class="form-control" value="@Model.Ingredients" />

    <label asp-for="ImagePath" class="control-label">Copy and paste the image url or the file path here for an Example Image:</label>
    <input id="TableRow" type="text" step="0.01" name="ImagePath" class="form-control" value="@Model.ImagePath" />

    <input type="submit" value="Update Recipe" class="btn btn-primary" />
}